[package]
name = "loonbox_extensions"
version = "0.1.0"
edition = "2021"
description = "LoonBox extension runtime — Lua and WASM plugin hosting"

[dependencies]
# mlua for Lua scripting (v1)
# mlua = { version = "0.9", features = ["lua54", "vendored", "async", "serialize"] }
# wasmtime for WASM extensions (v2)
# wasmtime = "25"
log = "0.4"
thiserror = "1.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
